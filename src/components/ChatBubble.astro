<div class="toast toast-end">
    <span id="dynamic-message" class="indicator-item badge badge-accent">🚀 work with us</span>
    <button class="btn btn-xs btn-outline btn-accent sm:btn-sm md:btn-md lg:btn-lg"> <!-- Aquí cambiamos btn-lg para agrandar el botón -->
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="h-8 w-8" 
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor">
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M7 8h10M7 12h8m-4 8l-4-4H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-3l-4 4z" />
      </svg>
    </button>
  </div>

  <script>
    const messages = ["🚀 work with us", "💬 chat with us", "📖 learn with us", "🤖 need help?"];
    let index = 0;
  
    setInterval(() => {
      const messageElement = document.getElementById("dynamic-message");
      index = (index + 1) % messages.length;
      messageElement.textContent = messages[index];
    }, 2300);
  </script>